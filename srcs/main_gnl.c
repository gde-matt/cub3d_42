#include "../includes/get_next_line.h"
#include "../includes/cub3d.h" 


int main(int argc, char *argv[])
{
    t_inputs    inputs = {0};

    process_input(argv[1], &inputs);
    //TODO: detectar a "flag" e armazenar as informações no struct;
    //TODO: uma vez que as informações foram passadas, armazenar o mapa;
    //TODO: Validar as informações (considerando inputs não perfeitos);
    //TODO: Validar mapa (considerando mapas inválidos);

    return (0);
}